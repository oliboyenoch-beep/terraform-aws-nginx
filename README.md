# Terraform Nginx Setup

This Terraform project:
- Creates an EC2 instance (Ubuntu 22.04 LTS)
- Installs Nginx automatically
- Clones a GitHub repository into `/var/www/html`

## Usage
1. Make sure Terraform and AWS CLI are installed.
2. Configure your AWS credentials with:
3. Initialize Terraform:
4. Plan and apply:

## Outputs
- EC2 Instance ID  
- Public IP or Elastic IP  
- Public DNS name

